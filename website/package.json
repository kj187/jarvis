{
  "name": "jarvis-website",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "pnpm run sync && vitepress dev --host 0.0.0.0 --port 5174",
    "build": "pnpm run sync && vitepress build",
    "preview": "vitepress preview --host 0.0.0.0 --port 5174"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
