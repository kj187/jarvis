---
description: Fully automated release — preflight, changelog, release notes, tag, push, CI monitoring
argument-hint: <version, e.g. 1.6.0 (optional — derived from commits if omitted)>
---

Target version: $ARGUMENTS

@.agents/release.md
