# Keep build context small and avoid copying host-installed deps/artifacts
# into images (which breaks pnpm's deps-status check inside the build).
**/node_modules
frontend/dist
backend/internal/static/dist
**/*.test.ts
**/*.test.tsx
playwright-report
test-results
.git
tmp
# Release/intro video work files (GBs of frames) — never part of an image build context
frontend/e2e/_video
# Docs website: regenerated content + build output, never part of an image build context
website/content
website/.vitepress/dist
website/.vitepress/cache
