clusters:
  - name: production
    alertmanagerUrl: https://alertmanager-internal.example.com
    auth:
      existingSecret: jarvis-upstream-auth
      existingSecretKeys:
        bearerToken: my-token-key
